spring.application.name=appointment
server.port=8080

# Read from ENV (DB_URL/DB_USER/DB_PASS). If not set, it falls back to local postgres.
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/healthcare}
spring.datasource.username=${DB_USER:healthcare_user}
spring.datasource.password=${DB_PASS:healthcare_pass}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false